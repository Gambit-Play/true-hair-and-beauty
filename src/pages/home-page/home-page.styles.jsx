import styled from 'styled-components';
import { globalStyles } from '../../themes/styled-components.theme';

export const Title = styled.div`
	${globalStyles.titles1};
`;

export const Body = styled.div`
	${globalStyles.body1};
`;
